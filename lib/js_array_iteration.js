// Js Array

const numbers = [1, 2, 3, 4];
 
console.log(numbers.length);

// Get array element by index
console.log(numbers[0]);

// Push an element into the js array
numbers.push(5);
console.log(numbers);


// Pop last elements from  an js array
numbers.pop();
console.log(numbers);









